```release-note:breaking-change
Improves markdown generation for the resource. Now the `default` description is placed before the `validators` descriptions. And now the `(ForceNew)` is colored red (Force new is generated by the hasicorp plan modifier `RequiresReplace`).
```